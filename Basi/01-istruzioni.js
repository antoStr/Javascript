// Esempi di istruzioni in Javascript e vengono considerate attive, quindi possono essere eseguite come istruzioni come per esempio assegnazione di una variabile, utilizzo di un metodo in un array o di una stringa etc.

let x = 1;
const y = 1;

"ciao".toUpperCase();
[10, 20, 30].push[40];

if (x == 1) {
  x += 1;
  x = 100;
}

// Tutto ciò che è messo nei commenti viene considerato inattivo e quindi ignorato dal programma, vengono utilizzati nel debug o commentare e documentare

// Commento a linea singola

/*
  Commento
  multi 
  linea
*/

/* Per visualizzare elementi, eseguire debug, comprensione del condice ed effettuare azioni rapidi come testare pezzi di codice etc puoi utilizzare il console.log
 */

console.log(x);
